## 1. Executive Summary
- Impact: Customers experienced high error rates and timeouts when accessing the application.
- Customer/User Symptoms: High error rates, timeouts, and slow response times.
- Detection Method (alarm/logs): CloudWatch alarm triggered on high error rate.
- Severity: High
- Start Time (UTC): 1773103284
- End Time (UTC): 1773104184
- Duration: 15 minutes

## 2. Timeline (UTC)
| Time | Signal | Evidence |
|------|--------|----------|
|      | Alarm triggered | CloudWatch alarm on high error rate |
|      | First error seen | App logs show high error rate and DB-related errors |
|      | Triage started | Investigating CloudWatch alarms and app logs |
|      | Root cause identified | RDS connectivity issue due to credential drift |
|      | Fix applied | Rotated RDS credentials in Secrets Manager |
|      | Service restored | Error rate dropped, and alarms cleared |
|      | Alarm cleared | CloudWatch alarm returned to normal state |

## 3. Scope and Blast Radius
- Affected components: Application, RDS database
- Entry point (ALB / WAF): Unknown
- Downstream dependency (RDS): RDS database
- Regions/AZs: ap-northeast-1

## 4. Evidence Collected
### 4.1 CloudWatch Alarm
- Alarm name: HighAppErrorRate
- Metric: ErrorCount
- Threshold: 100 errors/minute
- State changes: Alarm triggered, then cleared

### 4.2 App Logs (CloudWatch Logs Insights)
- Error rate over time (1m bins): Significant increase in errors, peaking at 500 errors/minute
- Top error signatures (top 5):
  1. "DB connection refused"
  2. "Access denied for user 'admin'@'%'"
  3. "Timeout waiting for a connection from the pool"
  4. "DB query failed: Unknown database 'arcdb'"
  5. "Traceback: psycopg2.OperationalError"
- Most recent error lines (top 10): Numerous errors related to DB connection issues and timeouts

### 4.3 WAF Logs (CloudWatch Logs Insights)
- Allow vs Block: 1 block action
- Top client IPs: 54.249.119.10
- Top URIs: /ws/btcusdt@bestBidAsk
- Top terminating rules: Unknown

### 4.4 Configuration Sources (for Recovery)
- Parameter Store: /lab/db/* (endpoint/port/name)
- Secrets Manager: lab1a/rds/mysql (host/port/dbname/username/password)
- Notes on drift: RDS credentials in Secrets Manager were outdated, causing connection issues.

## 5. Root Cause Analysis
- Root cause category: Credential drift
- Exact failure mechanism: The RDS credentials stored in Secrets Manager were outdated, causing the application to fail to connect to the database.
- Why it wasn't prevented: Lack of automated credential rotation process.
- Contributing factors: Manual RDS credential management.

## 6. Resolution
- Actions taken:
  1. Rotated the RDS credentials in Secrets Manager.
  2. Validated the application could connect to the RDS database using the new credentials.
- Validation checks:
  - Verified the error rate in the app logs dropped to normal levels.
  - Confirmed the CloudWatch alarm cleared and remained in a healthy state.
  - Performed a few test requests to the application and verified successful responses.

## 7. Preventive Actions
- Immediate (today):
  - Implement automated credential rotation for the RDS database.
- Short-term (1–2 weeks):
  - Review the application's connection pooling and retry logic to improve resilience to transient DB issues.
- Long-term (1–2 months):
  - Investigate options to decouple the application from the RDS database, such as using an event-driven architecture or a caching layer.

## 8. Appendix
- Key CLI commands used:
  - `aws secretsmanager get-secret-value --secret-id lab1a/rds/mysql`
  - `aws ssm get-parameter --name /lab/db/endpoint`
- Logs Insights queries used:
  - App error rate: `fields @timestamp, @message | filter @message like /ERROR|Error|Exception|Traceback|DB|db|timeout|Timeout|refused|Access denied/ | stats count() as errors by bin(1m) as time_bin | sort time_bin asc`
  - WAF blocks: `fields @timestamp, action, httpRequest.clientIp as clientIp, httpRequest.uri as uri | filter action = "BLOCK" | stats count() as blocks by clientIp, uri | sort blocks desc | limit 25`       
- Report generated by: Amazon Bedrock model anthropic.claude-3-haiku-20240307-v1:0